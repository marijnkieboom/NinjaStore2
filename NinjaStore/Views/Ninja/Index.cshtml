@model IEnumerable<NinjaStore.Data.Models.Ninja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NinjaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gold)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NinjaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gold)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.NinjaId }) |
                @Html.ActionLink("Details", "Details", new { id = item.NinjaId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.NinjaId })
            </td>
        </tr>
}
    </tbody>
</table>
